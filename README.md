# Phishing-URL-Detector
Architecture — Character-level CNN + Bidirectional LSTM with attention. The model reads URLs one character at a time, so it needs no pre-trained embeddings and generalises to brand-new domain tricks.
